const {Given, When, Then} = require('cucumber')

Given('The user clicks on the list button',function()
{
    
})

When('The button is clicked',function()
{
    
})

Then('The webpage should change to the Venue list page', function()
{

})